* Da fare sul client ogni ad ogni turno *

* Ci dovrebbe essere una libreria che permette di generare un pool di thread ed assegnare ad ogni pool una funzione*


children = *figli diretti della board*

move, points = nil, +-inf??

#assegno ad ogni ciclo 4 nodi a 4 thread, quando finiscono prendo il nodo con la mossa migliore
# e lo tengo come riferimento, poi eseguo il ciclo successivo con i prossimi 4 thread 

for i=0; i<len(children); i+=4:

	pool(4)

	move1,points1 = thread1.minmax(board.move(child0)...)
	move2,points2 = thread2.minmax(board.move(child1)...)
	move3,points3 = thread3.minmax(board.move(child2)...)
	move4,points4 = thread4.minmax(board.move(child3)...)

	#nella move alla fine ci sara' la mossa migliore 
	move = max(points, points1,2,3,4)

conn.send(move, turn)

#qualcosa del genere#
